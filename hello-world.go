/* 
every program needs to have package main and func main
func main is the entry point for all of your programs (main)
exit will be when flow of executions exits func main
*/

// packages can be thought like organizing with folders (group similar code together)
package main

import "fmt"

func main() {
	fmt.Println("Hello World")
}
